@* Bare layout for printable documents: no navigation drawer, no MudBlazor chrome, so the
   page contains the document and nothing else. Paged.js renders paper-sized pages into it. *@
@inherits LayoutComponentBase

<div class="wiq-print-shell">
    @Body
</div>
